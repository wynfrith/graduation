/**
 * Created by wyn on 3/25/16.
 */

const TagService = {

  // tag的增删改查

  createTag: (tag, parentId) => {
    
  },

  // 移除tag首先检查有没有子标签
  removeTag: (tagId) => {

  },

  // 递归移除子标签
  removeTagForce: (tagId) => {
    
  },

  updateTag: (tagId, tag) => {

  },

  getTagByName: (name) => {

  },
  
  getTagById: (tagId) => {
    
  },
  
  // 获取parentId下的所有tags
  getTags:(parentId) => {
    
  },
  
  getTagsCount: (parentId) => {
    
  }


};

export default TagService;